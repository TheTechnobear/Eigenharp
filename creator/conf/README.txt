# This directory contains config files that can be run using creator.sh (mac) or creator.cmd (windows)

# each directory represents a type of config, the most basic demostration rigs are contained in the sub directory basic
# (excluding basicdemo.conf, which used for the initial demo of functionality of creator)
# in the sub directory, configs will be for generic devices, any device specfic configs are help in a device subdirectory
# e.g.
# basic/basicmidi.conf - is suitable for all devices
# however, 
# basic/alpha/basicmidi.conf - would only be suitable for an alpha
