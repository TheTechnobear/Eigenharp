This directory contains config files that can be run using creator.sh (mac) or creator.cmd (windows)


directories:
this directory - be useable by all devices
alpha|tau|pico directories - are for those devices only 
experimental  - testing only, showing how to use certain templates (may or may not be tested)
layouts - simple configs, to change keyboard layouts
