# this directory is organised by functionality,
# basic contains top level functionality e.g. basic setups and keygroup control
# note rigs contains general rigs, but midi and audio units rig creation is in the relevant sub directory, along with its link commands

