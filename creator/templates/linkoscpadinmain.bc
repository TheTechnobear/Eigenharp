# this will add OSC pad to the main setup, this is temporary until we can put talkers in rigs
# VAR1=%VAR1% keygroup to link VAR2=%VAR2% kg output VAR3=%VAR3% keygroup channel


%VAR1% output %VAR2% to pad keygroup into channel %VAR3% connect
#pad keygroup output light to %VAR1% output keygroup %VAR3% light input connect 

%VAR1% output %VAR2% to pad talker keygroup  into channel %VAR3% connect
#pad keygroup output light to %VAR1% output keygroup %VAR3% light input connect 
