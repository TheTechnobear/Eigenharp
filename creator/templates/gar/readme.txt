# this directory contains templates for the green amber red switch structure
# as first seen in pico mix
# basic idea is its a 3 state switch that switches keygroup output, that allows 'pages' of menu functions
